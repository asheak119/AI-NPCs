{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Dialogue Response",
  "type": "object",
  "properties": {
    "spoken_text": {
      "type": "string",
      "description": "The actual dialogue spoken by the NPC to the player."
    },
    "tone_evaluation": {
      "type": "string",
      "description": "The emotional tone of the response, e.g., 'friendly', 'hostile', 'neutral', 'afraid'."
    },
    "engine_function_calls": {
      "type": "array",
      "description": "Optional game engine functions to call (e.g., set disposition, start quest).",
      "items": {
        "$ref": "function_call.schema.json"
      }
    }
  },
  "required": ["spoken_text", "tone_evaluation"]
}
